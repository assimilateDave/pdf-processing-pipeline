# === Elasticsearch Settings ===
ELASTICSEARCH_HOST=elasticsearch
ELASTICSEARCH_PORT=9200

# === App Settings ===
# The port your app runs on inside the container (adjust as needed)
APP_PORT=5000

# === File Input/Output Paths ===
# Set these to your input/output directories if your app uses them
INPUT_DIR=/data/input
OUTPUT_DIR=/data/output

# === Tesseract OCR Settings ===
# If you need to specify Tesseract path (adjust to your install location)
# TESSERACT_CMD=/usr/bin/tesseract

# === Other Application Settings ===
# Add any API keys, debug flags, etc.
# DEBUG=true
# SECRET_KEY=your_secret_key_here
